python -m venv .venv
.\.venv\Scripts\activate.ps1
pip install fastapi uvicorn pydantic pydantic-settings boto3
docker run -d -p 8000:8000 --name dynamodb amazon/dynamodb-local:2.4.0 -jar DynamoDBLocal.jar -sharedDb
python -m app.db.setup_db
uvicorn app.main:app  --port 8001 --reload
